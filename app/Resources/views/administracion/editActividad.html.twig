{# Pagina Nueva Actividad #}
{% extends 'administracion/base_admin.html.twig' %}

{# Usamos un theme para los formularios #}
{% form_theme form 'form/base_form.html.twig' %}

{# TITULO #}
{% block title %}Administracion Actividades{% endblock %}

{# TITULO BODY#}
{% block bodytitle %}
    {% if isEdit is defined and isEdit != false %}
        Edita Actividad
    {% else %}
        Nueva Actividad
    {% endif %}
{% endblock %}

{# CONTENT #}
{% block content %}

    {{ form_start(form) }}

    {#   esto dibuja los campos #}
    {{ form_widget(form) }}
    {# Esto es el ejemplo de renderizar manualmente cada campo del formulario
                    {{ form_errors(form) }}

                    <div class="control-group form-group">
                        {{ form_label(form.activity_name) }}
                        {{ form_errors(form.activity_name) }}
                        {{ form_widget(form.activity_name) }}
                    </div>

                    <div class="control-group form-group">
                        {{ form_widget(form.submit) }}
                    </div> #}
    {{ form_end(form) }}

{% endblock %}

{# CSS #}
{% block stylesheets %}
    {#<style>
        body { background: #F5F5F5; font: 18px/1.5 sans-serif; }
    </style>#}

    <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.6.0/css/bootstrap-datepicker.css">

{% endblock %}

{% block javascripts %}
    {# Aquí usaremos para las alertas la libreria sweetalert.js.org #}
    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
    <script src="{{ asset('js/sweetAlerts.js') }}" ></script>
    <script src="{{ asset('js/api.calls.js') }}" ></script>

    <script src="//cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.6.0/js/bootstrap-datepicker.min.js"></script>
    <script>
        jQuery(document).ready(function () {
            $('.js-datepicker').datepicker({
                format: 'yyyy-mm-dd'
            });
        });
    </script>
{% endblock %}



