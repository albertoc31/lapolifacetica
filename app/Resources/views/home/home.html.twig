{# Pagina Index #}
{% extends 'base.html.twig' %}

{# TITULO #}
{% block title %}La Polifacética Home{% endblock %}

{# BODY #}
{% block body %}

    <header>
        <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
            <ol class="carousel-indicators">
                <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
            </ol>
            <div class="carousel-inner" role="listbox">
                <!-- Slide One - Set the background image for this slide in the line below -->
                <div class="carousel-item active" style="background-image: url({{ asset('img/polifacetica_home-03.jpg') }})">
                    <div class="carousel-caption d-none d-md-block">
                        <h3>Un espacio vecinal en el Rastro</h3>
                        <p>Abierto, participativo y plural</p>
                    </div>
                </div>
                <!-- Slide Two - Set the background image for this slide in the line below -->
                <div class="carousel-item " style="background-image: url({{ asset('img/polifacetica_home-01.jpg') }})">
                    <div class="carousel-caption d-none d-md-block">
                        <h3>Un punto de encuentro</h3>
                        <p>Donde la diversidad nos hace crecer</p>
                    </div>
                </div>
                <!-- Slide Three - Set the background image for this slide in the line below -->
                <div class="carousel-item " style="background-image: url({{ asset('img/polifacetica_home-02.jpg') }})">
                    <div class="carousel-caption d-none d-md-block">
                        <h3>Con espíritu de conjunto</h3>
                        <p>Porque el todo es siempre más que la suma de las partes</p>
                    </div>
                </div>
            </div>
            <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </header>

    <!-- Page Content -->
    <div class="container">
        <h1 class="my-4">La Polifacética, <small> espacio de encuentro y participación</small></h1>
        <p>Las asociaciones, colectivos y personas que conformamos La Polifacética hemos confluido en un proyecto común para construir un espacio abierto, participativo y plural, donde la diversidad de actividades e intereses constituyan un bien común y una riqueza compartida.</p>
        <p>Con ello queremos promover la participación ciudadana, las iniciativas sociales y culturales, con valores de convivencia inclusiva, solidaridad, creatividad, equidad, sostenibilidad medioambiental y búsqueda del bien común.</p>
        <p>Desde este espacio organizamos talleres, encuentros, grupos de trabajo, ámbitos de intercambio vecinal y todo tipo de actividades e iniciativas de socialización y participación que contribuyan al desarrollo de las personas y el mejoramiento de las condiciones de vida en nuestros barrios.</p>
        <!-- Portfolio Section -->
        <h2>Actividades</h2>

        {#{% dump activities %}#}
        <div class="row">
            {% if activities is defined and activities is not empty %}
            {% for activity in activities %}
                <div class="col-lg-4 col-sm-6 portfolio-item">
                    <div class="card h-100">
                        <a href="{{ path('actividad', {'id':activity.id}) }}"><img class="card-img-top" src="{%- if activity.foto|length > 0 -%}{{ asset(activityImg ~ activity.foto) }}{%- else -%}http://placehold.it/700x400{%- endif -%}" alt=""></a>
                        <div class="card-body">
                            <h4 class="card-title">
                                <a href="{{ path('actividad', {'id':activity.id}) }}">{{ activity.activityName | capitalize}}</a>
                            </h4>
                            <p class="card-text">{{ activity.shortDescription }}</p>
                        </div>
                    </div>
                </div>
            {% endfor %}
            {% endif %}

        </div>
        <!-- /.row -->

        <button class="btn btn-primary"><a class="" href="{{ path('actividades', {'num':'1'}) }}">Ver más actividades</a></button>

        <!-- Pagination -->
        {#<ul class="pagination justify-content-center">
            {% if paginaActual > 1 %}
            <li class="page-item">
                <a class="page-link" href="{{ path('homepage', {pagina: paginaActual-1 < 1 ? 1 : paginaActual-1 }) }}" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                    <span class="sr-only">Previous</span>
                </a>
            </li>
            {% endif %}

            {% for i in 1..3 %}
                <li class="page-item">
                    <a class="page-link" href="{{ path('homepage', {pagina: i}) }}">{{i}}</a>
                </li>
            {% endfor %}

            {% if paginaActual < 3 %}
            <li class="page-item">
                <a class="page-link" href="{{ path('homepage', {pagina: paginaActual+1 }) }}" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                    <span class="sr-only">Next</span>
                </a>
            </li>
            {% endif %}
        </ul>#}

    </div>


{% endblock %}

{# CSS #}
{% block stylesheets %}
{#<style>
    body { background: #F5F5F5; font: 18px/1.5 sans-serif; }
</style>#}
{% endblock %}