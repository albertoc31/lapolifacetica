{# Pagina Index #}
{% extends 'base.html.twig' %}

{# TITULO #}
{% block title %}Actividades La Polifacética{% endblock %}

{# BODY #}
{% block body %}

    <!-- Page Content -->
    <div class="container">
        <h1 class="my-4">La Polifacética, <small> espacio de encuentro y participación</small></h1>
        <p>Las asociaciones, colectivos y personas que conformamos La Polifacética hemos confluido en un proyecto común para construir un espacio abierto, participativo y plural, donde la diversidad de actividades e intereses constituyan un bien común y una riqueza compartida.</p>
        <p>Con ello queremos promover la participación ciudadana, las iniciativas sociales y culturales, con valores de convivencia inclusiva, solidaridad, creatividad, equidad, sostenibilidad medioambiental y búsqueda del bien común.</p>
        <p>Desde este espacio organizamos talleres, encuentros, grupos de trabajo, ámbitos de intercambio vecinal y todo tipo de actividades e iniciativas de socialización y participación que contribuyan al desarrollo de las personas y el mejoramiento de las condiciones de vida en nuestros barrios.</p>
        <!-- Portfolio Section -->
        <h2>Actividades</h2>

        <div class="row">
            {% if activities is defined and activities is not empty %}
            {% for activity in activities %}
                <div class="col-lg-4 col-sm-6 portfolio-item">
                    <div class="card h-100">
                        <a href="{{ path('activity', {'id':activity.id}) }}"><img class="card-img-top" src="{%- if activity.foto|length > 0 -%}{{ asset(activityImg ~ activity.foto) }}{%- else -%}http://placehold.it/700x400{%- endif -%}" alt=""></a>
                        <div class="card-body">
                            <h4 class="card-title">
                                <a href="{{ path('activity', {'id':activity.id}) }}">{{ activity.activityName }}</a>
                            </h4>
                            <p class="card-text">{{ activity.shortDescription }}</p>
                        </div>
                    </div>
                </div>
            {% endfor %}
            {% endif %}

        </div>
        <!-- /.row -->

        <!-- Pagination -->
        <ul class="pagination justify-content-center">
            {% if paginaActual > 1 %}
            <li class="page-item">
                <a class="page-link" href="{{ path('homepage', {pagina: paginaActual-1 < 1 ? 1 : paginaActual-1 }) }}" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                    <span class="sr-only">Previous</span>
                </a>
            </li>
            {% endif %}

            {% for i in 1..3 %}
                <li class="page-item">
                    <a class="page-link" href="{{ path('homepage', {pagina: i}) }}">{{i}}</a>
                </li>
            {% endfor %}

            {% if paginaActual < 3 %}
            <li class="page-item">
                <a class="page-link" href="{{ path('homepage', {pagina: paginaActual+1 }) }}" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                    <span class="sr-only">Next</span>
                </a>
            </li>
            {% endif %}
        </ul>

    </div>


{% endblock %}

{# CSS #}
{% block stylesheets %}
{#<style>
    body { background: #F5F5F5; font: 18px/1.5 sans-serif; }
</style>#}
{% endblock %}